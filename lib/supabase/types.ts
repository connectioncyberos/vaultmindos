/**
 * TODO: substituir por tipos gerados automaticamente quando o Supabase
 * CLI estiver disponivel no ambiente de desenvolvimento:
 *
 *   npx supabase gen types typescript --project-id zgqcxllsnlkgpgzyfabd > lib/supabase/types.ts
 *
 * Ate la, este arquivo fica com um tipo minimo para nao quebrar imports.
 */
export type Database = Record<string, unknown>;
